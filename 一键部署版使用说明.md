# 拉格朗日考勤 · 一键版（exe）使用说明

> 给完全不懂技术的玩家：不用装 Python、不用敲命令，下载解压双击就能用。
> 开源 1.0 的「图片识别 + 自动考勤」能力，全部打包进了一个小程序。

## 一、怎么用（3 步）

1. **下载**：拿到 `拉格朗日考勤-一键版.zip`，解压到一个文件夹（比如桌面）。
2. **双击**：打开文件夹，双击 `拉格朗日考勤.exe`。
   - 它会**自动打开你的浏览器**，出现"拉格朗日考勤·一键版"界面。
   - 第一次运行会加载识别模型，可能等十几秒，正常现象。
3. **三步操作**：
   - **第 1 步 建名册**：选一张「全盟成员列表」截图（可多选）→ 点"生成名册"。
     生成后**核对一眼**（本地识别偶有形近字误差，改改错别字即可）。
     - ⭐ **不会丢人**：默认与已有名册**合并追加**，你分多次传图、反复生成，之前导入的人都还在。
       只有勾选"覆盖重建"才会清空旧名册从头来。
   - **第 1.5 步 手动编辑 / 录入名册**：OCR 认错名字、或想纯手填（不截图）都在这里改——
     网页里直接**增 / 删 / 改名 / 改小队**，点"保存名册"即可写入。小白零门槛。
   - **第 2 步 考勤打卡**：每次活动截一张图（纯名字名单 / 集合点星图都行）→ 填活动日期 → 点"开始考勤"。
     立刻出结果：谁到场、谁没来、连续/累计缺席、谁建议清退。
   - **第 3 步（可选）**：点"导出匿名案例包"，把样本分享给作者/社区，一起把识别做得更准。

结果可点"下载 Excel 表 / CSV"保存，直接发给队长。

## 二、要什么环境

- **Windows 7 及以上**（暂只发 Windows 版；Mac 走网页/管家版）。
- **不需要**装 Python、**不需要**联网（模型已打包在内，完全离线）。
- 关闭小程序 = 直接关掉那个黑窗口（或关浏览器标签页）。

## 三、常见问题

| 问题 | 说明 |
|---|---|
| 双击没反应 / 被杀毒软件拦 | 添加到杀毒软件白名单；这是本地工具不是病毒。 |
| 第一次很慢 | 在加载 OCR 模型，等十几秒即可。 |
| 名字认错了 | 本地 OCR 偶有形近字（如"群星"认成"群心"）。名册生成后手动改一下即可，越用越准。 |
| 想识别视频 / 自动定时出表 | 那是完整版能力；开源版只认图片。进群了解完整版。 |
| 数据安全吗 | 纯本地运行，截图只在你电脑上认名字，不上传、不连游戏、**零封号风险**。 |

## 四、开发者：如何自己打包（可选）

源码在 `attendance_gui.py`（本地网页 GUI）。打包成 exe：

```bash
pip install -r requirements.txt
pip install pyinstaller
pyinstaller build.spec
```

产物在 `dist/拉格朗日考勤/拉格朗日考勤.exe`（单文件夹，双击即用）。
打包时**务必确认不含真实 roster.csv / history/ 等隐私数据**再分发。

---

游戏名：飓人皇 · 微信：Chill_Thinker · GitHub: sque-h
个人非商用免费，商用或转售需作者授权。
